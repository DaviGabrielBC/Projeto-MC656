
name: Godot CI


on:

  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  gut-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Download Godot
        run: |
          wget https://github.com/godotengine/godot/releases/download/4.3-stable/Godot_v4.3-stable_linux.x86_64.zip -O godot.zip
          unzip godot.zip -d godot
          chmod +x godot/Godot_v4.3-stable_linux.x86_64
          godot --Version
      - name: Run GUT Tests
        run: |
          ls
          cd Jogo
          godot --Version
          godot -s addons/gut/gut_cmdln.gd -d --path "$PWD" -gdir=res://test/unit > gut_test_results.log
          cd ..
